before_install: 
   - cd tasks
   - npm install -g npm
language: node_js
env:
  global:
    - NODE_ENV=development
node_js:
  - 8.11.3
sudo: false
node_js:
  - "lts/*"
install:
  - npm install -g codecov
after_sucess:
  - npm run coverage
